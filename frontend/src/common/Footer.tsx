import React from "react"
import { Box } from "@mui/material"

export const Footer: React.FC = () => {
  return <Box sx={{ position: "fixed", bottom: 0, width: "100%" }}>(c) Vending Machine, Inc.</Box>
}
